<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quora Posts</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body {
      background-color: #f9f9f9;
      padding: 20px;
      text-align: center;
    }
    button:hover {
      background-color: #ddd;
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
    }

    .post {
      background: white;
      padding: 15px;
      border-radius: 10px;
      box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
      width: 60%;
      margin: 10px auto;
      text-align: left;
    }

    .user {
      color: #007bff;
      margin-bottom: 5px;
    }

    .content {
      color: #555;
    }

    .post a {
      display: inline-block;
      margin-top: 10px;
      padding: 8px 15px;
      background-color: #007bff;
      color: white;
      font-size: 14px;
      text-decoration: none;
      border-radius: 5px;
      transition: 0.3s;
    }

    .post a:hover {
      background-color: #0056b3;
    }

    .create-post {
      display: inline-block;
      margin-top: 20px;
      padding: 10px 20px;
      background-color: red;
      color: white;
      font-size: large;
      text-decoration: none;
      border-radius: 5px;
      transition: 0.3s;
    }
    
    .create-post:hover {
      background-color: darkred;
    }
  </style>
</head>
<body>
  <h1>Quora Posts</h1>
  <% for(post of posts){ %>
    <div class="post">
      <h2 class="user">@<%= post.username %></h2>
      <p class="content"><%= post.content %></p>
      <a href="/posts/<%=post.id%>">Click Me</a>
      <a href="/posts/<%=post.id%>/edit">Edit</a>
      <br>
      <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
        <button 
          style="display: inline-block;
          margin-top: 10px;
          cursor: pointer;
          padding: 8px 15px;
          background-color: #d50909;
          color: white;
          /* Removed invalid property 'hover' */
          font-size: 14px;
          text-decoration: none;
          border-radius: 5px;
          border: none;
          transition: 0.3s;">Delete</button>
      </form>
    </div>
  <% } %>

  <a class="create-post" href="/posts/new">Create the New Post</a>

</body>
</html>
